CONFIG_TARGET_x86=y
CONFIG_TARGET_x86_64=y
CONFIG_TARGET_x86_64_DEVICE_generic=y

# ==========================================
# 1. 基础架构与防火墙 (fw4 核心，适配 Nikki)
# ==========================================
CONFIG_PACKAGE_firewall4=y
CONFIG_PACKAGE_nftables=y

# ==========================================
# 2. 核心网络代理全家桶 (eBPF + 现代代理)
# ==========================================
CONFIG_PACKAGE_luci-app-nikki=y
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-app-mosdns=y
CONFIG_PACKAGE_luci-app-turboacc=y

# ==========================================
# 3. 满血 eBPF & BTF 支持
# ==========================================
CONFIG_KERNEL_DEBUG_INFO_BTF=y
CONFIG_KERNEL_DEBUG_INFO_BTF_MODULES=y
CONFIG_BPF=y
CONFIG_BPF_SYSCALL=y
CONFIG_BPF_JIT=y
CONFIG_PACKAGE_kmod-veth=y

# ==========================================
# 4. 极致网络吞吐：BBR 拥塞控制 + FQ 队列管理
# ==========================================
CONFIG_KERNEL_TCP_CONG_BBR=y
CONFIG_PACKAGE_kmod-sched-core=y
CONFIG_PACKAGE_kmod-sched=y
CONFIG_DEFAULT_TCP_CONG="bbr"

# ==========================================
# 5. N100 生产力必备：磁盘挂载与格式支持 (Samba/分区)
# ==========================================
# 磁盘分区与挂载核心工具
CONFIG_PACKAGE_luci-app-hd-idle=y
CONFIG_PACKAGE_luci-app-diskman=y
# 挂载必备文件系统格式驱动 (支持 ext4, NTFS, exFAT 等大容量移动硬盘)
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-fs-vfat=y
CONFIG_PACKAGE_kmod-fs-exfat=y
CONFIG_PACKAGE_kmod-fs-ntfs3=y
# 挂载管理服务
CONFIG_PACKAGE_automount=y

# ==========================================
# 6. 轻量服务器核心：Docker 支持 (极客最爱)
# ==========================================
# 开启 Docker 和 Portainer 图形化容器面板
CONFIG_PACKAGE_luci-app-dockerman=y
CONFIG_PACKAGE_luci-lib-docker=y

# ==========================================
# 7. 诊断与系统救砖必备工具 (全方位护航)
# ==========================================
# 基础测速
CONFIG_PACKAGE_luci-app-netspeedtest=y
# 网络调试与排错 (抓包、链路追踪、温度监控)
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_lm-sensors=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_nano=y
# 自动分配网卡中断到多核 (N100 2000M 核心优化)
CONFIG_PACKAGE_irqbalance=y
CONFIG_PACKAGE_kmod-shortcut-fe=y
CONFIG_PACKAGE_kmod-shortcut-fe-cm=y
